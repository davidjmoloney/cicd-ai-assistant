# CI/CD AI Assistant Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# GitHub Configuration (Required for PR Generation)
# =============================================================================

# GitHub Personal Access Token with repo permissions
GITHUB_TOKEN=ghp_your_token_here

# Target repository where PRs will be created
TARGET_REPO_OWNER=your-org
TARGET_REPO_NAME=your-repo

# Branch to create PRs against (default: main)
TARGET_REPO_DEFAULT_BRANCH=main

# =============================================================================
# PR Settings (Optional)
# =============================================================================

# Prefix for auto-generated branch names (default: ai-fix)
PR_BRANCH_PREFIX=ai-fix

# Comma-separated labels to add to PRs (default: ai-generated)
PR_LABELS=ai-generated

# Create PRs as drafts (default: false)
PR_DRAFT_MODE=false

# =============================================================================
# LLM Configuration (Required for Fix Generation)
# =============================================================================

# LLM provider: "openai" or "anthropic"
LLM_PROVIDER=openai

# OpenAI settings
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o

# Anthropic settings (if using anthropic provider)
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-sonnet-4-20250514
